# CMake Instructions

## For Linux
1. Run `cmake -B build`
2. Run `cd build && make`
3. Run `cmake --install build --prefix build/install`
4. (optional) Build examples

## For Windows

TODO: complete
